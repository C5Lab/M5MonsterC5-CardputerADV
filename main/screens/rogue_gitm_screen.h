/**
 * @file rogue_gitm_screen.h
 * @brief Rogue GITM wizard (scan → victim → uplink → connect → mirror → session)
 */

#ifndef ROGUE_GITM_SCREEN_H
#define ROGUE_GITM_SCREEN_H

#include "screen_manager.h"

screen_t *rogue_gitm_screen_create(void *params);

#endif /* ROGUE_GITM_SCREEN_H */
